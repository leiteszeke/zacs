// @flow

import createStyledElement, { div as view, span as text, button, img as image } from './web'

export type { StyledComponent, StyleClassNameProps, StyleObject } from './web/type'

export { createStyledElement, view, text, image, button }
